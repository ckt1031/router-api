{
	"name": "toupie",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"helper": "bun run scripts/helper.ts",
		"lint": "tsc --noEmit && biome lint",
		"lint:fix": "tsc --noEmit && biome check --write .",
		"deploy": "wrangler deploy --latest --minify --upload-source-maps",
		"validate": "bun run scripts/validate-config.ts",
		"clean-vercel": "bunx fix-esm-import-path .vercel/output",
		"vercel-deploy": "vercel build --prod && bun run clean-vercel && vercel deploy --prebuilt --prod",
		"postinstall": "bun run scripts/generate-schema.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.5",
		"@types/bun": "^1.2.23",
		"@types/cli-progress": "^3.11.6",
		"@types/lodash": "^4.17.20",
		"@types/node": "^24.6.2",
		"@vercel/node": "^5.3.26",
		"typescript": "^5.9.3",
		"vercel": "^48.2.8",
		"wrangler": "^4.42.0"
	},
	"dependencies": {
		"@hono/oidc-auth": "^1.7.0",
		"@inquirer/prompts": "^7.8.6",
		"cli-progress": "^3.12.0",
		"cli-table3": "^0.6.5",
		"hono": "^4.9.9",
		"lodash": "^4.17.21",
		"slugify": "^1.6.6",
		"zod": "^4.1.11"
	}
}
